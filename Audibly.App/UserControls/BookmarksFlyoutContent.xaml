<?xml version="1.0" encoding="utf-8" ?>

<UserControl
    mc:Ignorable="d"
    x:Class="Audibly.App.UserControls.BookmarksFlyoutContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Audibly.Models"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid HorizontalAlignment="Stretch" MinHeight="120" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                FontSize="16"
                FontWeight="SemiBold"
                Grid.Column="0"
                Text="Bookmarks"
                VerticalAlignment="Center" />

            <Button
                BorderThickness="0"
                Click="AddBookmarkButton_Click"
                Grid.Column="1"
                ToolTipService.ToolTip="Save bookmark at current position"
                x:Name="AddBookmarkButton">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;" />
            </Button>
        </Grid>

        <TextBlock
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Grid.Row="1"
            HorizontalAlignment="Center"
            Text="No bookmarks yet."
            VerticalAlignment="Center"
            Visibility="Collapsed"
            x:Name="EmptyText" />

        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind PlayerViewModel.CurrentBookmarks, Mode=OneWay}"
            MaxHeight="360"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.HorizontalScrollMode="Disabled"
            SelectionMode="None"
            x:Name="BookmarksList">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8,6" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Bookmark">
                    <Grid ColumnSpacing="8" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock
                                FontWeight="SemiBold"
                                MaxLines="3"
                                Text="{x:Bind Note, Mode=OneWay}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="Wrap" />
                            <TextBlock
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Loaded="LocationTextBlock_Loaded"
                                Tag="{x:Bind}" />
                        </StackPanel>

                        <StackPanel
                            Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="4"
                            VerticalAlignment="Center">
                            <Button
                                BorderThickness="0"
                                Click="PlayBookmark_Click"
                                Tag="{x:Bind}"
                                ToolTipService.ToolTip="Jump to position">
                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE768;" />
                            </Button>
                            <Button
                                BorderThickness="0"
                                ToolTipService.ToolTip="More">
                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE712;" />
                                <Button.Flyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem
                                            Click="EditBookmark_Click"
                                            Tag="{x:Bind}"
                                            Text="Edit note">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70F;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem
                                            Click="DeleteBookmark_Click"
                                            Tag="{x:Bind}"
                                            Text="Delete">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </Button.Flyout>
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
